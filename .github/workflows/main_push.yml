name: Automatic ci/cd Backend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: Deploy to server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "bash -lc '
            set -euo pipefail
      
            # Go to app directory
            cd /home/${{ secrets.SSH_USER }}/${{ secrets.SSH_PROJECTNAME }}

            # Update repo safely
            git pull origin main

            # Activate virtualenv
            source .venv/bin/activate

            # Restart app with pm2
            pm2 restart ${{ secrets.SSH_PROJECTNAME }}
            pm2 restart ${{ secrets.SSH_DJANGO }}
          '"
  
